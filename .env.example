# Public configuration only. Set this to the deployed API Gateway base URL.
# Never put AWS credentials or Google OAuth secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
